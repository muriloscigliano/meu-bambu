---
---

<header class="topbar">
	<div class="topbar__container">
		<a href="#comprar" class="topbar__cta" data-hover-scale>COMPRAR</a>
	</div>
</header>

<style>
.topbar { position: fixed; inset-inline: 0; top: 0; z-index: 50; background: color-mix(in oklab, var(--color-beige) 90%, transparent); backdrop-filter: blur(6px); }
.topbar__container { max-width: var(--container-max); margin-inline: auto; height: 64px; display: flex; align-items: center; justify-content: flex-end; padding-inline: 1.5rem; }
.topbar__cta { display: inline-block; background: var(--color-brand); color: #fff; padding: .5rem 1rem; border-radius: 9999px; font-size: .875rem; }
</style>


