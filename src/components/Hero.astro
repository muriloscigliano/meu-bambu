---
interface Props {
	title: string;
	subtitle?: string;
	ctaHref?: string;
	ctaLabel?: string;
}

const { title, subtitle, ctaHref = '#comprar', ctaLabel = 'Comprar agora' } = Astro.props as Props;
---

<section class="hero">
	<div class="hero__container" data-reveal>
		<h1 class="hero__title">PAINÉIS DE BAMBU<br />PREMIUM</h1>
		<p class="hero__desc">Os únicos painéis de bambu no Brasil com núcleo vertical. Mais fortes, estáveis e duradouros que os tradicionais painéis horizontais. O material ideal para móveis, marcenaria e design sustentável.</p>
		<div class="hero__divider"></div>
		<div class="hero__pills">
			<span class="pill">Entrega rápida</span>
			<span class="pill">Pagamento seguro</span>
			<span class="pill">Qualidade garantida</span>
		</div>
		<a href={ctaHref} class="hero__cta" data-hover-scale>ESCOLHER MEU PAINEL</a>
	</div>
</section>

<style>
.hero { position: relative; isolation: isolate; min-height: 90svh; display: grid; place-items: center; text-align: center; padding-top: 6rem; background: var(--color-beige); margin-block: var(--section-gap); border-top: 1px solid var(--color-line); border-bottom: 1px solid var(--color-line); }
.hero__container { max-width: var(--container-max); margin-inline: auto; padding-inline: 1.5rem; }
.hero__title { font-family: var(--font-display); font-weight: 800; text-transform: uppercase; line-height: 1.05; letter-spacing: -0.02em; font-size: clamp(2.5rem, 5vw, 3.75rem); }
.hero__desc { margin: 1rem auto 0; max-width: 48rem; color: #4b5563; font-size: .95rem; }
.hero__divider { margin: 1.25rem auto 0; height: 1px; width: min(80%,640px); background: var(--color-line); }
.hero__pills { margin-top: 1rem; display: flex; gap: .5rem; justify-content: center; }
.pill { display: inline-flex; align-items: center; border: 1px solid var(--color-line); border-radius: 9999px; padding: .375rem .75rem; text-transform: uppercase; font-size: 11px; letter-spacing: .14em; background: color-mix(in oklab, var(--color-beige) 70%, white 30%); }
.hero__cta { display: inline-block; margin-top: 2rem; background: var(--color-brand); color: #fff; padding: .75rem 1.5rem; border-radius: 9999px; }
</style>


